type Query {
  """@require <guest> Current visitor must be a guest."""
  value: String!
}

"""
Description.

@require <guest> Current visitor must be a guest.
"""
type Test {
  """@require <guest> Current visitor must be a guest."""
  one: String!

  """@require <guest> Current visitor must be a guest."""
  two: String!

  """@require <guest> Current visitor must be a guest."""
  extension: String!
}
