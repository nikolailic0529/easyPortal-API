type Query {
    value: String! @authOrganization @mock

    root: Test! @authOrganization(root: true) @mock
}

"""
Description.
"""
type Test @authOrganization {
    one: String!

    two: String! @authOrganization

    root: String! @authOrganization(root: true)
}

extend type Test @authOrganization {
    extension: String!

    extensionRoot: String! @authOrganization(root: true)
}
