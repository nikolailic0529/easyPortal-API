type Query {
  value: String! @organization @mock

  root: Test! @organization(root: true) @mock
}

"""
Description.
"""
type Test @organization {
  one: String!

  two: String! @organization

  root: String! @organization(root: true)
}

extend type Test @organization {
  extension: String!

  extensionRoot: String! @organization(root: true)
}
