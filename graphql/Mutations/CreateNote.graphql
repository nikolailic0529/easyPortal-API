extend type Mutation {
  createNote(
    input: CreateNoteInput!
  ): CreateNotePayload
  @me
  @organization
}

input CreateNoteInput @validator {
  note: String!
  document_id: String!
  files: [Upload!]
}

type CreateNotePayload {
  created: Note!
}
