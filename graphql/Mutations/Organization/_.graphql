extend type Mutation {
    organization(id: ID @eq): OrganizationMutations
    @authOrg(root: true)
    @authMe(permissions: ["administer"])
    @mutation(
        model: "App\\Models\\Organization"
    )
}

type OrganizationMutations
