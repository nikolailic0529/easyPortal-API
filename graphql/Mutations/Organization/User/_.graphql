extend type OrganizationMutations {
    user(id: ID @eq): OrganizationUserMutations
    @authOrg(root: true)
    @authMe(permissions: ["administer"])
    @mutation(
        relation: "users"
    )
}

type OrganizationUserMutations
