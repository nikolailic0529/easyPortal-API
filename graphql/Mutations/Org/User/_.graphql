extend type OrgMutations {
    user(id: ID @eq): OrgUserMutations
    @authOrganization
    @authMe(permissions: ["org-administer"])
    @mutation(
        relation: "users"
    )
}

type OrgUserMutations
