extend type Mutation {
    org: OrgMutations
    @authOrganization
    @authMe(permissions: ["org-administer"])
    @mutation(
        resolver: "App\\GraphQL\\Queries\\Org"
    )
}

type OrgMutations
