extend type CustomerMutations {
    changeRequest: CustomerChangeRequestMutations
    @authOrg
    @authMe
    @mutation(
        relation: "changeRequests"
    )
}

type CustomerChangeRequestMutations
