extend type Mutation {
    contract(id: ID @eq): ContractMutations
    @authOrganization
    @authMe
    @builder(
        method: "App\\GraphQL\\Queries\\Contracts\\Contracts"
    )
    @mutation(
        model: "App\\Models\\Document"
    )
}

type ContractMutations
