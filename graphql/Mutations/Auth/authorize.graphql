extend type Mutation {
  "Finish the Sign In."
  authorize(
    input: AuthorizeInput
  ): AuthorizePayload
    @field(resolver: "App\\GraphQL\\Mutations\\Auth\\Authorize")
    @guest
}

input AuthorizeInput {
  code: String!
  state: String!
}

type AuthorizePayload {
  me: Me
}
