extend type Mutation {
    user(id: ID @eq): UserMutations
    @authOrganization(root: true)
    @authMe(permissions: ["administer"])
    @mutation(
        model: "App\\Models\\User"
    )
}

type UserMutations
