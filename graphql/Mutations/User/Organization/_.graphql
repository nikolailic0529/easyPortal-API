extend type UserMutations {
    organization(id: ID @eq(key: "organization_id")): UserOrganizationMutations
    @organization(root: true)
    @me(permissions: ["administer"])
    @mutation(
        relation: "organizations"
    )
}

type UserOrganizationMutations
