extend type DocumentMutations {
  sync(
    input: [DocumentSyncInput!]!
  ): DocumentSyncPayload
  @organization(root: true)
  @me(permissions: ["administer"])
  @field(resolver: "App\\GraphQL\\Mutations\\Document\\Sync")
}

input DocumentSyncInput {
  id: ID!
}

type DocumentSyncPayload {
  result: Boolean!
}
