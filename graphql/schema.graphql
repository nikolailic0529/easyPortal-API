#import Scalars/*.graphql

type Query {
    "Current user, `null` for guests."
    me: User
}

type User {
    id: ID! # FIXME [!] Do we really need it on frontend?
    given_name: String!
    family_name: String!
}
