type ApplicationJobProgress {
    name: String
    total: Int
    processed: Int
    success: Int
    failed: Int
    updated: Int
    created: Int
    ignored: Int
    deleted: Int
    current: Boolean
    operations: [ApplicationJobProgress!]
}
