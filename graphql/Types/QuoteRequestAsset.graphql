type QuoteRequestAsset {
    id: ID!
    asset_id: ID!
    asset: Asset! @relation
    duration_id: ID!
    duration: QuoteRequestDuration! @relation
    service_level_custom: String
    service_level_id: ID
    serviceLevel: ServiceLevel @relation
}
