type Me {
  id: ID!
  given_name: String!
  family_name: String!
  locale: String
  root: Boolean!
  searches (key: String!): [MeSearch!]!
    @field(resolver: "App\\GraphQL\\Queries\\UserSearches")
    @guard
}
