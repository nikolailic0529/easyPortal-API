type Me {
  id: ID!
  given_name: String!
  family_name: String!
  locale: String
  root: Boolean!
    @field(resolver: "App\\GraphQL\\Queries\\Me@root")
  searches (
    where: MeSearchQuery @searchBy
    order: MeSearchSort @sortBy
  ): [MeSearch!]!
    @hasMany
}
