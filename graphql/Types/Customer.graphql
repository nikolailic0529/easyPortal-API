type Customer {
    id: ID!
    name: String!
    type: CustomerType!
    status: CustomerStatus!
    locations_count: Int!
    contacts_count: Int # Will be update when creating contacts
    locations: [Location]! @morphMany
}
