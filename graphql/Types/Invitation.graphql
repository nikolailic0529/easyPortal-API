type Invitation {
    id: ID!
    organization_id: ID!
    role_id: ID!
    role: Role @relation
    team_id: ID
    team: Team @relation
    email: Email!
    used_at: DateTime
    expired_at: DateTime!
}
