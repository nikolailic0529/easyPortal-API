type User {
  id: ID!
  given_name: String
  family_name: String
  email: String!
  email_verified: Boolean!
  enabled: Boolean!
  invitations: [Invitation!]! @hasMany

  "Team in the current organization."
  team: Team @hasOne

  "Role in the current organization."
  role: Role @hasOne
}
