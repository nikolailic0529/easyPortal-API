type Audit {
    id: ID!
    organization_id: ID
    user_id: ID
    object_type: String
    object_id: String
    action: String!
    created_at:  DateTime!

    context: String
    @field(resolver: "App\\GraphQL\\Queries\\AuditContext")

    user: User
    organization: Organization
}
