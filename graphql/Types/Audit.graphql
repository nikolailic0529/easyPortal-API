type Audit {
    id: ID!
    organization_id: ID
    user_id: ID
    object_type: String
    object_id: String
    action: String!
    created_at:  DateTime!

    "JSON"
    context: String
    @field(resolver: "App\\GraphQL\\Resolvers\\JsonResolver")

    user: User
    organization: Organization
}
