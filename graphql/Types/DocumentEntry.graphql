type DocumentEntry {
  id: ID!
  document_id: ID!
  net_price: String
  list_price: String
  discount: String
  renewal: String
  asset_id: ID!
  asset: Asset! @belongsTo
  service_id: ID!
  service: Product! @belongsTo
  currency_id: ID
  currency: Currency @belongsTo
}
